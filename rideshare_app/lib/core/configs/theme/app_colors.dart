import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xff08B783);

  static const secondprimary = Color(0xff008955);

  static const background = Color(0xffffffffffff);

  static const secondBackground = Color.fromARGB(255, 232, 231, 236);

  /// Material grey swatch — use instead of [Colors.grey] across the app.
  static const grey = Color(0xFF9E9E9E);
  static const grey400 = Color(0xFFBDBDBD);
  static const grey600 = Color(0xFF757575);
  static const grey700 = Color(0xFF616161);

  /// Secondary text, hints, and borders.
  static const textColor = grey400;
}
